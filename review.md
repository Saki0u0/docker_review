# Webについて
---
### プロトコルとは何か説明してください。
コンピュータ同士が通信を行う際のルールや規約のこと。


### httpとhttpsの違いについて説明してください。
HTTPは通信内容が暗号化されていないプロトコルであり、第三者に内容を盗み見られる可能性がある。
一方HTTPSはSSL/TLSという暗号化技術を使用して通信を保護するため、安全にデータの送受信を行うことができる。


### Webサーバがどのような役割を持っているかについて説明してください。
Webサーバは、クライアント（主にWebブラウザ）からのリクエストを受け取り、HTMLやCSS、画像などのコンテンツを返す役割と必要に応じてアプリケーションを実行し、その結果をレスポンスとして返す役割を担ってる。


### クライアントとサーバの違いについて説明してください。
クライアントはサーバに対してリクエストを送信する側であり、サーバはそのリクエストを受け取り、必要な処理を行った後にレスポンスを返す役割を持つ。


### URLにアクセスして動的なウェブサイトの画面が表示されるまでの流れを説明してください。
ユーザーがブラウザにURLを入力すると、クライアントがDNSを利用してドメイン名からIPアドレスを取得し、そのIPアドレスを持つサーバへリクエストを送信します。
サーバはリクエストを受け取ると、必要に応じてアプリケーションを実行し、データベースから情報を取得します。その後、生成されたHTMLなどのレスポンスをクライアントへ返し、ブラウザがそれを解析して画面を表示します。



# Apacheについて
---
### Apacheとはどのようなソフトウェアか説明してください。
Apacheは、クライアントからのHTTPリクエストを受け取り、Webページや画像などのコンテンツを返すWebサーバソフトウェア。動的なWebページを生成するためにPHPなどのアプリケーションと連携することも可能。


### DocumentRootがどのような役割を持ったディレクティブか説明してください。
DocumentRootは、Webサーバが公開するファイルを配置するディレクトリを指定するディレクティブ。
クライアントからリクエストがあった際に、WebサーバはDocumentRootで指定されたディレクトリを基準にファイルを探してレスポンスを返す。


### DirectoryIndexがどのような役割を持ったディレクティブか説明してください。
DirectoryIndexは、ディレクトリにアクセスされた際に自動的に表示するファイルを指定するディレクティブ。
通常はindex.htmlやindex.phpなどが設定されており、ユーザーがファイル名を指定しなくてもWebページが表示される。




# Dockerについて
---
### Dockerとはどのようなソフトウェアか説明してください。
Dockerは、アプリケーションの実行環境をコンテナとしてまとめ、どの環境でも同じように動作させることができるコンテナ型の仮想化ソフトウェア。
これにより、環境差異による問題を防ぎ、効率的に開発や運用を行うことができる。


### Docker-Composeとはどのようなソフトウェアか説明してください。
Docker Composeは、複数のコンテナをまとめて定義・管理し、一括で起動や停止を行うことができるツール。


### コンテナとはどのようなものか説明してください。
コンテナとは、アプリケーションとその実行に必要な環境をまとめて隔離した実行単位。
ホストOS上で独立して動作するため、他のコンテナの影響を受けず、同じ環境をどこでも再現することが可能。


### Dockerにおけるイメージとはどのようなものか説明してください。
Dockerにおけるイメージとは、コンテナを作成するためのテンプレートのようなものであり、アプリケーションや必要なライブラリ、設定などが含まれている。
このイメージをもとにコンテナが生成され、同じ環境を再現することが可能。


### DockerにおいてDockerfileは何のために使用するか説明してください。
Dockerfileは、Dockerイメージを作成するための手順を記述した設定ファイル。
使用するベースイメージや必要なソフトウェアのインストール、環境設定などをコードとして管理することで、同じ環境を再現できるようにしている。


### Dockerのボリュームマウントは何のために使用するか説明してください。
Dockerのボリュームマウントは、ホストOSとコンテナ間でファイルやデータを共有し、永続化するために使用。
これにより、コンテナを削除してもデータを保持できるほか、ホスト側で編集したファイルをコンテナに即座に反映させることが可能。



# ネットワークについて
---
### ドメインとは何か説明してください。
ドメインとは、インターネット上のコンピュータを識別するための名前であり、IPアドレスを文字列で表したものです。
ドメイン名はDNSによってIPアドレスに変換され、目的のサーバへアクセスできるようになる。


### ホスト名とは何か説明してください。
ホスト名とは、ネットワーク上の個々のコンピュータやサーバを識別するために付けられた名前。
ドメイン名と組み合わせることで、どのサーバにアクセスするかを特定することが可能。


### ポートとは何か説明してください。
ポートとは、1台のコンピュータ上でどのアプリケーションと通信するかを識別するための番号。
IPアドレスが通信先のコンピュータを特定するのに対し、ポート番号はその中のどのサービスに接続するかを指定する役割を持つ。


### ポートフォワードとは何か説明してください。
ポートフォワードとは、特定のポートに届いた通信を別のポートや別のコンピュータへ転送する仕組みのこと。
これにより、外部から内部ネットワーク上のサービスへアクセスが可能になる。


### 127.0.0.1というIPアドレスが何か説明してください。
127.0.0.1は、自分自身のコンピュータを指すIPアドレスであり、「ループバックアドレス」と呼ばれます。
このアドレスに通信を行うと外部のネットワークには出ず、自分の端末内で通信が完結する。


### localhostとは何か説明してください。
localhostとは、自分自身のコンピュータを指すホスト名のことで、通常はIPアドレス「127.0.0.1」に対応。
主にローカル環境で動作確認や開発を行う際に使用され、外部のネットワークを経由せずに通信が完結。




